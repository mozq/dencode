<%@ page contentType="text/html; charset=UTF-8" pageEncoding="UTF-8"
%><h3>Об удалении дубликатов строк</h3>
<p>Удаление дубликатов строк убирает повторяющиеся строки из многострочного текста. Если одна и та же строка встречается больше одного раза, сохраняется только первое вхождение, а последующие удаляются.</p>
<p>Оставшиеся строки сохраняют исходный порядок. Текст при этом не сортируется.</p>

<p>Например, если удалить дубликаты строк из следующего текста:</p>

<pre>
apple
banana
apple
orange
banana
</pre>

<p>Результат будет таким:</p>

<pre>
apple
banana
orange
</pre>

<div class="table-responsive">
	<table class="table">
		<tr><th scope="col">Входная строка</th><th scope="col">Результат</th><th scope="col">Описание</th></tr>
		<tr><td><code>apple</code> (1-й раз)</td><td>Остается</td><td>Это первое вхождение строки.</td></tr>
		<tr><td><code>banana</code> (1-й раз)</td><td>Остается</td><td>Это первое вхождение строки.</td></tr>
		<tr><td><code>apple</code> (2-й раз)</td><td>Удалено</td><td>Такая строка уже встречалась.</td></tr>
		<tr><td><code>orange</code> (1-й раз)</td><td>Остается</td><td>Это первое вхождение строки.</td></tr>
		<tr><td><code>banana</code> (2-й раз)</td><td>Удалено</td><td>Такая строка уже встречалась.</td></tr>
	</table>
</div>

<p>Для поиска дубликатов сравнивается вся строка целиком. Различия в регистре, пробелы в начале или конце, а также полноширинные и полуширинные символы считаются разным текстом.</p>
