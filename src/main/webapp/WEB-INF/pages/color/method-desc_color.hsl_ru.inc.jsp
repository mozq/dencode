<%@ page contentType="text/html; charset=UTF-8" pageEncoding="UTF-8"
%><%@ taglib prefix="dc" uri="http://dencode.com/jsp/taglib"
%><h3>О цвете HSL</h3>
<p>Цвет HSL — это цветовая модель, представляющая цвета с помощью оттенка, насыщенности и светлоты. В отличие от RGB, где напрямую задаётся интенсивность красного, зелёного и синего света, HSL разделяет тон, яркость цвета и светлоту, поэтому удобен для настройки цветов и работы с CSS.</p>

<div class="table-responsive">
	<table class="table">
		<thead>
			<tr><th>Компонент</th><th>Значение</th><th>Пример значения</th></tr>
		</thead>
		<tbody>
			<tr><td>H: Оттенок</td><td>Обозначает оттенок как угол. Красный находится около 0deg, зелёный около 120deg, синий около 240deg.</td><td><code>90deg</code></td></tr>
			<tr><td>S: Насыщенность</td><td>Обозначает насыщенность цвета. 0% — ахроматический цвет, 100% — самый насыщенный цвет.</td><td><code>50%</code></td></tr>
			<tr><td>L: Светлота</td><td>Обозначает светлоту цвета. 0% — чёрный, 50% — обычная светлота, 100% — белый.</td><td><code>50%</code></td></tr>
		</tbody>
	</table>
</div>

<p>Например, основные цвета, такие как красный, можно представить следующим образом.</p>

<div class="table-responsive">
	<table class="table">
		<thead>
			<tr><th>Цвет</th><th>Запись HSL</th><th>Запись RGB</th></tr>
		</thead>
		<tbody>
			<tr><td>Красный</td><td><code>hsl(0deg 100% 50%)</code></td><td><code>rgb(255 0 0)</code></td></tr>
			<tr><td>Зелёный</td><td><code>hsl(120deg 100% 25.1%)</code></td><td><code>rgb(0 128 0)</code></td></tr>
			<tr><td>Лайм</td><td><code>hsl(120deg 100% 50%)</code></td><td><code>rgb(0 255 0)</code></td></tr>
			<tr><td>Синий</td><td><code>hsl(240deg 100% 50%)</code></td><td><code>rgb(0 0 255)</code></td></tr>
			<tr><td>Белый</td><td><code>hsl(0deg 0% 100%)</code></td><td><code>rgb(255 255 255)</code></td></tr>
			<tr><td>Чёрный</td><td><code>hsl(0deg 0% 0%)</code></td><td><code>rgb(0 0 0)</code></td></tr>
		</tbody>
	</table>
</div>

<p>Чтобы добавить прозрачность, укажите альфа-значение. Например, полупрозрачный красный можно представить как <code>hsl(0deg 100% 50% / 0.5)</code>.</p>

<p>В качестве входных данных можно использовать запись с пробелами <code>hsl(0deg 100% 50%)</code>, а также старую запись с запятыми <code>hsl(0deg, 100%, 50%)</code>.</p>

<h4>Обработка оттенка, насыщенности и светлоты</h4>
<p>Оттенок обрабатывается как угол; значения больше 360deg и отрицательные значения нормализуются в диапазон от 0deg до 360deg. Например, <code>hsl(360deg 100% 50%)</code> считается тем же цветом, что и <code>hsl(0deg 100% 50%)</code>, а <code>hsl(-120deg 100% 50%)</code> — тем же цветом, что и <code>hsl(240deg 100% 50%)</code>.</p>

<p>Насыщенность и светлота обрабатываются в диапазоне от 0% до 100%. При насыщенности 0% цвет становится ахроматическим серым независимо от оттенка. При светлоте 0% цвет чёрный, при 100% — белый.</p>

<h4>Разница между HSL и RGB</h4>
<p><a href="${dc:h(basePath)}/color/rgb">RGB</a> напрямую задаёт красный, зелёный и синий компоненты, поэтому близок к отображению на экране и данным изображения. HSL разделяет оттенок, насыщенность и светлоту, упрощая операции вроде «сделать светлее при том же оттенке» или «уменьшить только насыщенность».</p>

<p>Однако HSL не является перцепционно равномерным цветовым пространством. Когда нужно удобнее работать с визуальной светлотой или различиями цветов, могут использоваться такие цветовые пространства, как <a href="${dc:h(basePath)}/color/lab">Lab</a>, <a href="${dc:h(basePath)}/color/lch">LCH</a>, <a href="${dc:h(basePath)}/color/oklab">Oklab</a> и <a href="${dc:h(basePath)}/color/oklch">Oklch</a>.</p>
