<%@ page contentType="text/html; charset=UTF-8" pageEncoding="UTF-8"
%><%@ taglib prefix="dc" uri="http://dencode.com/jsp/taglib"
%><h3>О цвете HWB</h3>
<p>Цвет HWB — это цветовая модель, которая описывает цвет тремя компонентами: оттенком, белизной и чернотой. Оттенок задает базовый цветовой тон, а белизна и чернота показывают, сколько белого и черного добавлено. Поэтому осветление, затемнение и приближение цвета к серому описываются достаточно наглядно.</p>

<div class="table-responsive">
	<table class="table">
		<thead>
			<tr><th>Компонент</th><th>Значение</th><th>Пример значения</th></tr>
		</thead>
		<tbody>
			<tr><td>H: Оттенок</td><td>Представляет цветовой тон как угол. Красный находится около 0deg, зеленый около 120deg, синий около 240deg.</td><td><code>90deg</code></td></tr>
			<tr><td>W: Белизна</td><td>Показывает количество белого, смешанного с цветом. Чем больше значение, тем белее цвет.</td><td><code>20%</code></td></tr>
			<tr><td>B: Чернота</td><td>Показывает количество черного, смешанного с цветом. Чем больше значение, тем темнее цвет.</td><td><code>30%</code></td></tr>
		</tbody>
	</table>
</div>

<p>Например, основные цвета, такие как красный, можно представить следующим образом.</p>

<div class="table-responsive">
	<table class="table">
		<thead>
			<tr><th>Цвет</th><th>Запись HWB</th><th>Запись RGB</th></tr>
		</thead>
		<tbody>
			<tr><td>Красный</td><td><code>hwb(0deg 0% 0%)</code></td><td><code>rgb(255 0 0)</code></td></tr>
			<tr><td>Зеленый</td><td><code>hwb(120deg 0% 49.8%)</code></td><td><code>rgb(0 128 0)</code></td></tr>
			<tr><td>Лайм</td><td><code>hwb(120deg 0% 0%)</code></td><td><code>rgb(0 255 0)</code></td></tr>
			<tr><td>Синий</td><td><code>hwb(240deg 0% 0%)</code></td><td><code>rgb(0 0 255)</code></td></tr>
			<tr><td>Белый</td><td><code>hwb(0deg 100% 0%)</code></td><td><code>rgb(255 255 255)</code></td></tr>
			<tr><td>Черный</td><td><code>hwb(0deg 0% 100%)</code></td><td><code>rgb(0 0 0)</code></td></tr>
		</tbody>
	</table>
</div>

<p>Чтобы включить прозрачность, добавьте альфа-значение. Например, полупрозрачный красный можно записать как <code>hwb(0deg 0% 0% / 0.5)</code>.</p>

<h4>Обработка оттенка, белизны и черноты</h4>
<p>Оттенок обрабатывается как угол, а значения больше 360deg или отрицательные значения нормализуются в диапазон от 0deg до 360deg. Например, <code>hwb(360deg 0% 0%)</code> обрабатывается как тот же цвет, что и <code>hwb(0deg 0% 0%)</code>, а <code>hwb(-120deg 0% 0%)</code> — как тот же цвет, что и <code>hwb(240deg 0% 0%)</code>.</p>

<p>Белизна и чернота обрабатываются в диапазоне от 0% до 100%. Чем больше белизна, тем ближе цвет к белому; чем больше чернота, тем ближе он к черному. Если сумма белизны и черноты равна 100% или больше, цвет обрабатывается как сероватый независимо от оттенка.</p>

<h4>Разница между HWB и HSL</h4>
<p><a href="${dc:h(basePath)}/color/hsl">HSL</a> регулирует цвет с помощью насыщенности и светлоты, поэтому подходит для задания яркости и светлоты. HWB регулирует цвет количеством добавленного белого и черного, поэтому легко описывает операции вроде «сделать белее при том же оттенке» или «добавить черный, чтобы затемнить».</p>

<p>Однако HWB также не является перцептуально равномерным цветовым пространством. Если нужно удобнее работать с видимой яркостью или различиями цветов, можно использовать цветовые пространства, такие как <a href="${dc:h(basePath)}/color/lab">Lab</a>, <a href="${dc:h(basePath)}/color/lch">LCH</a>, <a href="${dc:h(basePath)}/color/oklab">Oklab</a> и <a href="${dc:h(basePath)}/color/oklch">Oklch</a>.</p>
