<%@ page contentType="text/html; charset=UTF-8" pageEncoding="UTF-8"
%><h3>О шифре Плейфера</h3>
<p>Шифр Плейфера — это шифр замены, который заменяет пары букв с помощью таблицы букв 5×5.</p>

<p>Например, если ключ — "SECRET", а открытый текст — "HELLO", шифрование выглядит так.</p>

<pre>
Открытый текст : HELLO
Шифртекст      : ISKYIQ
</pre>

<p>При шифровании открытый текст сначала разбивается на пары букв. Если в паре две одинаковые буквы, например "LL", между ними вставляется вспомогательная буква "X". Если в конце остается одна буква, также добавляется "X".</p>

<pre>
HE L<b>X</b> LO
</pre>

<p>Затем подготавливается таблица шифрования 5×5, всего 25 ячеек. Поскольку в алфавите 26 букв, обычно "I=J" помещают в одну и ту же ячейку.</p>
<p>Сначала в таблицу записываются буквы ключа без повторов. Для ключа "SECRET" вторая "E" пропускается. Затем добавляется "ABCDEFGHIKLMNOPQRSTUVWXYZ"; буквы, уже присутствующие в ключе, также пропускаются. Так получается таблица 5×5 без повторяющихся букв.</p>

<pre>
S E C R T
A B D F G
H I K L M
N O P Q U
V W X Y Z
</pre>

<p>С помощью этой таблицы каждая пара букв шифруется по следующим правилам. Способ зависит от положения двух букв в таблице.</p>

<ul>
	<li>В одной строке: каждая буква заменяется буквой справа. Если буква находится у правого края, переход выполняется к левому краю.</li>
	<li>В одном столбце: каждая буква заменяется буквой под ней. Если буква находится внизу, переход выполняется наверх.</li>
	<li>В разных строках и столбцах: представьте прямоугольник, углами которого являются эти две буквы, и замените их буквами из двух других углов.</li>
</ul>

<pre>
S <strong>E</strong>
A B
<strong>H</strong> I

K <strong>L</strong>
P Q
<strong>X</strong> Y

I K <strong>L</strong>
<strong>O</strong> P Q
</pre>

<pre>
HE LX LO
↓
IS KY IQ
</pre>

<p>При расшифровании шифртекст обрабатывается парами с помощью той же таблицы букв. Обратите внимание, что вспомогательная буква "X" автоматически не удаляется.</p>

<pre>
Шифртекст      : ISKYIQ
Открытый текст : HEL<strong>X</strong>LO
</pre>
