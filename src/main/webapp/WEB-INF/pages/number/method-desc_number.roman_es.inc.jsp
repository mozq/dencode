<%@ page contentType="text/html; charset=UTF-8" pageEncoding="UTF-8"
%><h3>Acerca de los números romanos</h3>
<p>Los números romanos representan enteros combinando los símbolos <code>I</code>, <code>V</code>, <code>X</code>, <code>L</code>, <code>C</code>, <code>D</code> y <code>M</code>.</p>

<p>La notación romana estándar puede representar enteros del 1 al 3999. No existe un símbolo para el 0.</p>
<pre>
1234 = MCCXXXIV
3999 = MMMCMXCIX
</pre>

<h4>Símbolos básicos</h4>
<p>Los números romanos usan los siguientes siete símbolos.</p>

<div class="table-responsive">
	<table class="table">
		<thead>
			<tr><th>Símbolo</th><th>Valor</th></tr>
		</thead>
		<tbody>
			<tr><td><code>I</code></td><td>1</td></tr>
			<tr><td><code>V</code></td><td>5</td></tr>
			<tr><td><code>X</code></td><td>10</td></tr>
			<tr><td><code>L</code></td><td>50</td></tr>
			<tr><td><code>C</code></td><td>100</td></tr>
			<tr><td><code>D</code></td><td>500</td></tr>
			<tr><td><code>M</code></td><td>1000</td></tr>
		</tbody>
	</table>
</div>

<h4>Notación aditiva</h4>
<p>Cuando los símbolos se escriben de mayor a menor valor, sus valores se suman.</p>
<pre>
VIII = 5 + 1 + 1 + 1 = 8
LX = 50 + 10 = 60
MDC = 1000 + 500 + 100 = 1600
</pre>

<p><code>I</code>, <code>X</code>, <code>C</code> y <code>M</code> pueden repetirse hasta tres veces seguidas. <code>V</code>, <code>L</code> y <code>D</code> se usan solo una vez en la misma posición decimal.</p>

<h4>Notación sustractiva</h4>
<p>Cuando un símbolo menor se coloca justo antes de uno mayor, se resta el valor menor. La notación romana estándar usa las siguientes seis formas sustractivas.</p>

<div class="table-responsive">
	<table class="table">
		<thead>
			<tr><th>Notación</th><th>Valor</th></tr>
		</thead>
		<tbody>
			<tr><td><code>IV</code></td><td>4</td></tr>
			<tr><td><code>IX</code></td><td>9</td></tr>
			<tr><td><code>XL</code></td><td>40</td></tr>
			<tr><td><code>XC</code></td><td>90</td></tr>
			<tr><td><code>CD</code></td><td>400</td></tr>
			<tr><td><code>CM</code></td><td>900</td></tr>
		</tbody>
	</table>
</div>

<pre>
44 = XLIV
999 = CMXCIX
1999 = MCMXCIX
</pre>

<p>Formas como <code>IC</code> para 99, <code>IL</code> para 49 o <code>VX</code> para 5 no son estándar, por lo que no se admiten.</p>

<h4>Historia de los números romanos</h4>
<p>Se cree que los números romanos se desarrollaron en la antigua Roma a partir de marcas simples y muescas usadas para contar. Un trazo vertical como <code>I</code> llegó a representar 1, otros símbolos representaron grupos de 5 y 10, y el sistema terminó organizándose con combinaciones de <code>V</code>, <code>X</code>, <code>L</code>, <code>C</code>, <code>D</code> y <code>M</code>.</p>

<p>Los números romanos antiguos no estaban tan estrictamente normalizados como hoy. Por ejemplo, el 4 podía escribirse como <code>IIII</code> además de <code>IV</code>, y el 9 a veces como <code>VIIII</code> en lugar de <code>IX</code>. Las formas sustractivas actuales, como <code>IV</code>, <code>IX</code>, <code>XL</code>, <code>XC</code>, <code>CD</code> y <code>CM</code>, se estandarizaron más tarde.</p>

<p>Los números romanos se usaron durante mucho tiempo en el comercio, inscripciones, calendarios, relojes y números de capítulos. Hoy permanecen como una notación decorativa y convencional para años y numeraciones.</p>
