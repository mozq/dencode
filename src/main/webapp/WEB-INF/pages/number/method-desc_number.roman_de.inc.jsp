<%@ page contentType="text/html; charset=UTF-8" pageEncoding="UTF-8"
%><h3>Über römische Zahlen</h3>
<p>Römische Zahlen stellen ganze Zahlen dar, indem die Zeichen <code>I</code>, <code>V</code>, <code>X</code>, <code>L</code>, <code>C</code>, <code>D</code> und <code>M</code> kombiniert werden.</p>

<p>Mit der üblichen römischen Schreibweise können ganze Zahlen von 1 bis 3999 dargestellt werden. Für 0 gibt es kein Zeichen.</p>
<pre>
1234 = MCCXXXIV
3999 = MMMCMXCIX
</pre>

<h4>Grundzeichen</h4>
<p>Römische Zahlen verwenden die folgenden sieben Zeichen.</p>

<div class="table-responsive">
	<table class="table">
		<thead>
			<tr><th>Zeichen</th><th>Wert</th></tr>
		</thead>
		<tbody>
			<tr><td><code>I</code></td><td>1</td></tr>
			<tr><td><code>V</code></td><td>5</td></tr>
			<tr><td><code>X</code></td><td>10</td></tr>
			<tr><td><code>L</code></td><td>50</td></tr>
			<tr><td><code>C</code></td><td>100</td></tr>
			<tr><td><code>D</code></td><td>500</td></tr>
			<tr><td><code>M</code></td><td>1000</td></tr>
		</tbody>
	</table>
</div>

<h4>Additive Schreibweise</h4>
<p>Wenn Zeichen von größeren zu kleineren Werten angeordnet werden, werden ihre Werte addiert.</p>
<pre>
VIII = 5 + 1 + 1 + 1 = 8
LX = 50 + 10 = 60
MDC = 1000 + 500 + 100 = 1600
</pre>

<p><code>I</code>, <code>X</code>, <code>C</code> und <code>M</code> dürfen höchstens dreimal hintereinander stehen. <code>V</code>, <code>L</code> und <code>D</code> werden in derselben Stellenposition nur einmal verwendet.</p>

<h4>Subtraktive Schreibweise</h4>
<p>Steht ein kleineres Zeichen direkt vor einem größeren Zeichen, wird der kleinere Wert abgezogen. Die übliche römische Schreibweise verwendet die folgenden sechs subtraktiven Formen.</p>

<div class="table-responsive">
	<table class="table">
		<thead>
			<tr><th>Schreibweise</th><th>Wert</th></tr>
		</thead>
		<tbody>
			<tr><td><code>IV</code></td><td>4</td></tr>
			<tr><td><code>IX</code></td><td>9</td></tr>
			<tr><td><code>XL</code></td><td>40</td></tr>
			<tr><td><code>XC</code></td><td>90</td></tr>
			<tr><td><code>CD</code></td><td>400</td></tr>
			<tr><td><code>CM</code></td><td>900</td></tr>
		</tbody>
	</table>
</div>

<pre>
44 = XLIV
999 = CMXCIX
1999 = MCMXCIX
</pre>

<p>Schreibweisen wie <code>IC</code> für 99, <code>IL</code> für 49 oder <code>VX</code> für 5 sind nicht standardmäßig und werden daher nicht unterstützt.</p>

<h4>Geschichte der römischen Zahlen</h4>
<p>Römische Zahlen entstanden vermutlich im alten Rom aus einfachen Zählzeichen und Kerben. Ein senkrechter Strich wie <code>I</code> stand für 1, eigene Zeichen wurden für Gruppen von 5 und 10 verwendet, und daraus entwickelte sich ein System aus Kombinationen von <code>V</code>, <code>X</code>, <code>L</code>, <code>C</code>, <code>D</code> und <code>M</code>.</p>

<p>Frühe römische Zahlen waren nicht so streng vereinheitlicht wie heute. So wurde 4 nicht nur als <code>IV</code>, sondern auch als <code>IIII</code> geschrieben, und 9 manchmal als <code>VIIII</code> statt <code>IX</code>. Die heute üblichen subtraktiven Formen wie <code>IV</code>, <code>IX</code>, <code>XL</code>, <code>XC</code>, <code>CD</code> und <code>CM</code> wurden später standardisiert.</p>

<p>Römische Zahlen wurden lange im Handel, in Inschriften, Kalendern, Uhren und Kapitelnummern verwendet. Heute bleiben sie als dekorative und konventionelle Schreibweise für Jahreszahlen und Nummerierungen erhalten.</p>
