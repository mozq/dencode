<%@ page contentType="text/html; charset=UTF-8" pageEncoding="UTF-8"
%><h3>About Roman numerals</h3>
<p>Roman numerals represent integers by combining the symbols <code>I</code>, <code>V</code>, <code>X</code>, <code>L</code>, <code>C</code>, <code>D</code>, and <code>M</code>.</p>

<p>Standard Roman numerals can represent integers from 1 to 3999. There is no symbol for 0.</p>
<pre>
1234 = MCCXXXIV
3999 = MMMCMXCIX
</pre>

<h4>Basic symbols</h4>
<p>Roman numerals use the following seven symbols.</p>

<div class="table-responsive">
	<table class="table">
		<thead>
			<tr><th>Symbol</th><th>Value</th></tr>
		</thead>
		<tbody>
			<tr><td><code>I</code></td><td>1</td></tr>
			<tr><td><code>V</code></td><td>5</td></tr>
			<tr><td><code>X</code></td><td>10</td></tr>
			<tr><td><code>L</code></td><td>50</td></tr>
			<tr><td><code>C</code></td><td>100</td></tr>
			<tr><td><code>D</code></td><td>500</td></tr>
			<tr><td><code>M</code></td><td>1000</td></tr>
		</tbody>
	</table>
</div>

<h4>Additive notation</h4>
<p>When symbols are written from larger values to smaller values, their values are added.</p>
<pre>
VIII = 5 + 1 + 1 + 1 = 8
LX = 50 + 10 = 60
MDC = 1000 + 500 + 100 = 1600
</pre>

<p><code>I</code>, <code>X</code>, <code>C</code>, and <code>M</code> may be repeated up to three times in a row. <code>V</code>, <code>L</code>, and <code>D</code> are used only once in the same digit position.</p>

<h4>Subtractive notation</h4>
<p>When a smaller symbol is placed immediately before a larger symbol, the smaller value is subtracted. Standard Roman numerals use the following six subtractive forms.</p>

<div class="table-responsive">
	<table class="table">
		<thead>
			<tr><th>Notation</th><th>Value</th></tr>
		</thead>
		<tbody>
			<tr><td><code>IV</code></td><td>4</td></tr>
			<tr><td><code>IX</code></td><td>9</td></tr>
			<tr><td><code>XL</code></td><td>40</td></tr>
			<tr><td><code>XC</code></td><td>90</td></tr>
			<tr><td><code>CD</code></td><td>400</td></tr>
			<tr><td><code>CM</code></td><td>900</td></tr>
		</tbody>
	</table>
</div>

<pre>
44 = XLIV
999 = CMXCIX
1999 = MCMXCIX
</pre>

<p>Forms such as <code>IC</code> for 99, <code>IL</code> for 49, or <code>VX</code> for 5 are not standard, so they are not supported.</p>

<h4>History of Roman numerals</h4>
<p>Roman numerals are thought to have developed in ancient Rome from simple marks and tally-like notches used for counting. A vertical stroke such as <code>I</code> came to represent 1, separate symbols came to represent groups of 5 and 10, and the system was eventually organized around combinations of <code>V</code>, <code>X</code>, <code>L</code>, <code>C</code>, <code>D</code>, and <code>M</code>.</p>

<p>Early Roman numerals were not as strictly standardized as they are today. For example, 4 was sometimes written as <code>IIII</code> as well as <code>IV</code>, and 9 was sometimes written as <code>VIIII</code> instead of <code>IX</code>. The subtractive forms commonly used today, such as <code>IV</code>, <code>IX</code>, <code>XL</code>, <code>XC</code>, <code>CD</code>, and <code>CM</code>, were standardized later.</p>

<p>Roman numerals were used for a long time in commerce, inscriptions, calendars, clocks, and chapter numbers. They remain in use today as decorative and conventional notation for years and numbering.</p>
