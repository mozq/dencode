<%@ page contentType="text/html; charset=UTF-8" pageEncoding="UTF-8"
%><h3>關於羅馬數字</h3>
<p>羅馬數字透過組合 <code>I</code>、<code>V</code>、<code>X</code>、<code>L</code>、<code>C</code>、<code>D</code>、<code>M</code> 等符號來表示整數。</p>

<p>標準羅馬數字可以表示 1 至 3999 的整數。沒有表示 0 的符號。</p>
<pre>
1234 = MCCXXXIV
3999 = MMMCMXCIX
</pre>

<h4>基本符號</h4>
<p>羅馬數字使用以下7種符號。</p>

<div class="table-responsive">
	<table class="table">
		<thead>
			<tr><th>符號</th><th>值</th></tr>
		</thead>
		<tbody>
			<tr><td><code>I</code></td><td>1</td></tr>
			<tr><td><code>V</code></td><td>5</td></tr>
			<tr><td><code>X</code></td><td>10</td></tr>
			<tr><td><code>L</code></td><td>50</td></tr>
			<tr><td><code>C</code></td><td>100</td></tr>
			<tr><td><code>D</code></td><td>500</td></tr>
			<tr><td><code>M</code></td><td>1000</td></tr>
		</tbody>
	</table>
</div>

<h4>加法記數</h4>
<p>當符號依照由大至小的值排列時，會將各符號的值相加。</p>
<pre>
VIII = 5 + 1 + 1 + 1 = 8
LX = 50 + 10 = 60
MDC = 1000 + 500 + 100 = 1600
</pre>

<p><code>I</code>、<code>X</code>、<code>C</code>、<code>M</code> 最多可以連續使用3次。<code>V</code>、<code>L</code>、<code>D</code> 在同一位上只使用1次。</p>

<h4>減法記數</h4>
<p>將較小的符號放在較大的符號之前時，會減去較小的值。標準羅馬數字使用以下6種減法形式。</p>

<div class="table-responsive">
	<table class="table">
		<thead>
			<tr><th>記法</th><th>值</th></tr>
		</thead>
		<tbody>
			<tr><td><code>IV</code></td><td>4</td></tr>
			<tr><td><code>IX</code></td><td>9</td></tr>
			<tr><td><code>XL</code></td><td>40</td></tr>
			<tr><td><code>XC</code></td><td>90</td></tr>
			<tr><td><code>CD</code></td><td>400</td></tr>
			<tr><td><code>CM</code></td><td>900</td></tr>
		</tbody>
	</table>
</div>

<pre>
44 = XLIV
999 = CMXCIX
1999 = MCMXCIX
</pre>

<p>將 <code>IC</code> 視為 99、<code>IL</code> 視為 49、<code>VX</code> 視為 5 的寫法並非標準寫法，因此不支援。</p>

<h4>羅馬數字的歷史</h4>
<p>羅馬數字被認為起源於古羅馬用於計數的簡單標記和刻痕。像 <code>I</code> 這樣的直線逐漸表示 1，5 和 10 的組合亦開始用不同符號表示，最終整理成組合 <code>V</code>、<code>X</code>、<code>L</code>、<code>C</code>、<code>D</code>、<code>M</code> 的記數體系。</p>

<p>早期羅馬數字並不像現在這樣嚴格統一。例如，4 除了寫作 <code>IV</code>，亦會寫作 <code>IIII</code>；9 有時亦會寫作 <code>VIIII</code>，而不是 <code>IX</code>。現在常見的 <code>IV</code>、<code>IX</code>、<code>XL</code>、<code>XC</code>、<code>CD</code>、<code>CM</code> 等減法形式，是後來整理為標準寫法的。</p>

<p>羅馬數字曾長期用於商業交易、碑文、曆法、時鐘、章節編號等場合。今天，它們仍作為表示年份和編號的裝飾性、慣用性寫法保留下來。</p>
